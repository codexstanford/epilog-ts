function normalizeSymbols(queryList) {
    let symbolStrSet = new Set();
    for (let q of queryList) {
        // Gather symbols 
    }
    // For each symbol, generate a new 
    return;
}
export {};
//# sourceMappingURL=normalization.js.map